spring.application.name=core

# Database Configuration 
spring.datasource.url=jdbc:postgresql://localhost:5432/tariff_db
# spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/tariff_db}
spring.datasource.username=admin
# spring.datasource.username=${DATABASE_USERNAME:tariff_user}
spring.datasource.password=admin123
# spring.datasource.password=${DATABASE_PASSWORD:tariff_dev_password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect